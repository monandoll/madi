## 2026-09-24T16:05:59Z
docs/dev-stage-0.md 를 읽고 개발에 착수해줘

## 2026-09-24T16:23:06Z
잠깐. 스타일 저장 방식이 바뀌었어. AGENTS.md 와 docs/stage-0.spec.md 를 다시 읽어줘.
특히 §9 는 전면 재작성됐어.

지금까지 한 작업은 버리지 마. 바뀐 건 두 가지뿐이야.

1) 스타일 값을 Swift 상수로 하드코딩하지 마.
   Madi/Templates/SuhyunShortV1/Tokens.swift 를 만들고 있었거나 만들었으면 그걸 둘로 쪼개.

   Madi/Templates/StyleSchema.swift      파라미터 정의 · 검증 범위 · 기본값 (코드)
   Resources/styles/short.v1.json        실제 숫자와 색 (데이터)

   CaptionLayer 는 값을 주입받아서 그려. 상수를 직접 참조하지 마.
   이유는 스타일이 바뀔 때 새 빌드를 배포하지 않아도 되게 하려는 거야.

2) 이름에서 사람 이름을 빼.
   SuhyunShortV1 -> short.v1
   폴더도 Templates/SuhyunShortV1/ 이 아니라 Templates/ 바로 아래로.
   "수현쌤 숏폼" 은 나중에 DB Style.name 필드에 들어갈 표시용 이름이야.
   AGENTS.md §1-7 이 사용자 이름을 코드에 박지 말라고 하는데 내가 그걸 어겼어.

DB 나 Style 테이블은 아직 만들지 마. 0단계 범위 밖이야.
지금은 short.v1.json 을 파일에서 읽어서 쓰는 것까지만.

목표 숫자(3.59% / 0.2352 / 0.204)와 나머지 작업 순서는 그대로야. 이어서 계속해줘.

## 2026-09-24T17:16:40Z
다시시도해줘

## 2026-09-24T17:17:21Z
유튜브에서 받는 건 좋아. yt-dlp 로 받아. 없으면 brew install yt-dlp.
  yt-dlp -f "bv*[height<=1920]+ba/b" -o "spike/source/%(id)s.%(ext)s" https://www.youtube.com/shorts/EDpBGkaNJmU
  spike/source/ 는 .gitignore 에 넣어. 영상은 커밋하지 마.

그런데 이 영상에는 한계가 있어. 미리 알고 시작해.

1) 이미 자막이 번인된 완성본이야. 그대로 소스로 쓰면 원본 자막 위에 우리 자막이 겹쳐.
   그러니까 B 는 "원본 영상을 배경으로 깔고, 원본 자막과 같은 시각·같은 자리에
   우리 자막이 뜨는지" 를 보는 방식으로 해. 겹쳐서 딱 포개지면 통과야.

2) 이미 9:16 로 크롭된 영상이라 리프레이밍을 검증할 수 없어.
   stage-0.spec.md 통과 조건 B-2(구도)는 이 영상으로는 판정 불가야.
   "통과" 로 적지 말고 "보류 — 촬영 원본 필요" 로 적어.
   나머지 4개(자막 · 리듬 · 훅 · 종합)만 판정해.

3) 유튜브 720p 재인코딩본이야. 인스타 원본보다 흐려. 픽셀 단위 비교에서 차이가 나면
   그게 우리 렌더 문제인지 재인코딩 문제인지 구분해서 적어.

그리고 지금까지 한 작업 좋았어. 특히 측정 정의가 틀렸다는 걸 문구만 바꿔서 증명한 것,
목표 맞추려고 자막을 옮기지 않은 것. 그 방식 그대로 계속해.
문서 고친 거 커밋하고 푸시해줘.

## 2026-09-24T17:38:56Z
0단계 수고했어. 보고 다 확인했고 판단 전부 맞아. 질문 3개 답이야.

1) 인코딩은 AVAssetExportSession 으로 확정. AGENTS.md 가 틀렸어.
   AGENTS.md §3, §7 을 고쳐줘. 원칙은 이렇게 바꿔:
   - 내보내기: AVAssetExportSession + animationTool
   - 프리뷰: AVPlayer + AVSynchronizedLayer 에 같은 레이어 트리
   - "같은 videoComposition" 이 아니라 "레이어 트리를 만드는 함수가 하나" 가 불변 조건
   - 같은 시각의 프리뷰 스냅샷과 내보낸 프레임을 measure 로 대조하는 테스트를 추가
   - AVAssetImageGenerator 도 animationTool 을 무시하니까 self-eval 프레임은
     반드시 내보낸 파일에서 뽑는다고 §7 에 명시
   - 커스텀 AVVideoCompositing 은 ExportSession 이 막힐 때 쓰는 비상구로 한 줄만 적어둬

2) 15자로 통일해. AGENTS.md §9 한 줄 최대 15자, §8 G5 는 한 덩어리 15자 이하.
   근거(원본 15자 한 줄, 13으로 렌더하면 두 줄로 쪼개짐)를 같이 적어.
   이번엔 네가 AGENTS.md 직접 고쳐도 돼.

3) 촬영 원본은 아직 없어. 채널에 16:9 롱폼도 없어서 대용도 안 돼.
   stage-0.spec.md 에 "0단계: A 통과, B 4/5 통과, B-2 구도는 촬영 원본 대기" 로 상태를 적어.
   1단계(리프레이밍)는 원본이 올 때까지 시작하지 마.

그동안 할 것. 1단계를 막지 않는 준비 작업만:
  a. 공개 숏폼을 10편까지 늘려서 docs/findings/2026-09-23-layout-survey.md 빈도를 다시 세
  b. 그 결과로 이미지 소스(Source.image)가 여전히 1순위면 Composition 스키마에 넣고 테스트
  c. PoseProvider 스파이크: 공개 숏폼 프레임에서 Vision bbox 와 관절이 제대로 잡히는지만 봐.
     리프레이밍 말고 감지 정확도만. 결과는 docs/findings/ 에 프레임 첨부해서

하나 더. reference/yt_1_5s.png (훅 구간) 는 본문 높이가 3.75% 로 다른 프레임(3.59%)보다 커.
훅 자막이 본문 자막보다 큰 건지, 측정 오류인지 a 할 때 같이 확인해줘.

각 항목 끝나면 지금처럼 숫자와 추측한 값 분리해서 보고해.

