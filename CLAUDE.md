# 마디 (madi)

이 저장소의 개발 지침은 `AGENTS.md` 한 곳에만 있다. Claude Code와 Codex가 같은 문서를 읽는다.
내용을 이 파일로 복사하지 않는다. 지침을 고칠 때는 항상 `AGENTS.md`를 고친다.

@AGENTS.md

## 작업 시작 전 확인

1. 지금 몇 단계인가? (`AGENTS.md §12`) 통과 조건을 만족하지 않은 단계를 건너뛰지 않는다.
2. 지금 고치려는 게 `§0 반복 금지 목록`에 걸리는가?
3. 렌더 출력을 바꾸는 작업인가? 그러면 프레임 시트를 뽑아 눈으로 확인하고 커밋에 첨부한다.
